import Link from 'next/link';
import { ArrowLeft, ArrowUpRight } from 'lucide-react';
type ProjectPageProps={world:string; kicker:string; intro:string; title:string; description:string; image:string; capabilities:string[]; steps:string[]; accent:string};
export function ProjectPage({world,kicker,intro,title,description,image,capabilities,steps,accent}:ProjectPageProps){ return <main><section className="min-h-[100svh] px-5 md:px-8 pt-32 pb-10 flex flex-col justify-end" style={{background:accent}}><Link href="/#work" className="absolute top-24 left-5 md:left-8 link-arrow text-xs uppercase tracking-[.15em] text-black/65"><ArrowLeft size={14}/> All worlds</Link><p className="eyebrow text-black/60 mb-8">{kicker}</p><h1 className="display-sm text-black max-w-6xl">{world}</h1><p className="text-black/70 mt-10 max-w-xl text-lg md:text-2xl leading-tight">{intro}</p></section><section className="px-5 md:px-8 py-28 md:py-40"><p className="eyebrow mb-6">Featured project</p><h2 className="display-sm">{title}</h2><p className="text-xl md:text-3xl max-w-3xl mt-10 leading-tight text-white/70">{description}</p><div className="grid lg:grid-cols-[1.2fr_.8fr] gap-14 mt-20 items-start"><div className="min-h-[420px] md:min-h-[650px] overflow-hidden bg-white/5 relative"><img src={image} alt={`${title} project visual`} className="absolute inset-0 w-full h-full object-cover opacity-80"/></div><div><p className="eyebrow mb-5">Capabilities</p><ul className="border-t hairline">{capabilities.map(c=><li key={c} className="py-4 border-b hairline text-sm text-white/70">{c}</li>)}</ul></div></div></section><section className="px-5 md:px-8 py-28 border-t hairline"><p className="eyebrow mb-12">From image to intelligence</p><div className="grid md:grid-cols-5 gap-3">{steps.map((s,i)=><div key={s} className="border hairline p-5 min-h-28 flex flex-col justify-between"><span className="eyebrow">0{i+1}</span><span className="text-sm">{s}</span></div>)}</div><Link href="/#contact" className="link-arrow mt-14 uppercase tracking-[.14em] text-sm" style={{color:accent}}>Start a conversation <ArrowUpRight size={16}/></Link></section></main> }
